# next

## 创建页面

1. 页面就是 pages 目录下的一个组件
2. static 目录映射静态文件
3. page 具有特殊静态方法 `getInitialProps`

```sh
yarn add next react react-dom
vim package.json
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start"
  }
```
